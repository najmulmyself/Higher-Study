import 'package:flutter/material.dart';

class EngCollegeList extends StatelessWidget {
  const EngCollegeList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}